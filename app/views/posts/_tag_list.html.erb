<div class="tag-list ml-3">
    <% tag_list.each do |tag| %>
        <span class="badge badge-info mr-2">
            <%= link_to tag, posts_path(tag_name: tag), class: "text-white" %>
        </span>
    <% end %>
</div>